export enum ModeEnum {
   edit = 'edit',
   create = 'create',
}

export enum FolderEnum {
   category = 'category',
   product = 'product',
}

export enum PropertyTypeEnum {
   numeric = 1,
   text = 2,
   list = 3,
}
